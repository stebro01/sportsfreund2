
export default class Workout {
  constructor (label, maxSet, currentSet, addMin, addMax, ID) {
    this.label = label
    this.maxSet = maxSet
    this.currentSet = currentSet
    this.addMin = addMin
    this.addMax = addMax
    this.ID = ID
  }
}
